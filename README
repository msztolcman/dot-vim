My own vim config.

Functions:
    function! FindSubs()
    function! Home ()
    function! InArray (value, array)
    function! NameCaseCamelToUnderscore ()
    function! NameCaseUnderscoreToCamel ()
    function! SetOverLength()
    function! SafeCreateDir (dirpath)
    function! StripSpaces(...)
    function! ToggleFold()
    function! ToggleNameCase ()
    function! ToggleOption (option)
    function! ToggleRelativeAbsoluteNumber()
    function! s:ToggleYesNo()

Mappings:
    ,fd - make 'cd:' to dirname () of current file
    ,j - fuzzy finder per line (FufLine)
    ,l - switch relative/absolute line numbering
    ,n - disable search hilghlighting
    ,pwd - the same as :pwd
    ,r - read ~/.vimxfer file and put content below (look at ,w)
    ,t - switch boolean (manyu different types, look at s:ToggleYesNo())
    ,w - save selected text to external file (~/.vimxfer). Usefull when need to copy large part of data between vim instances. Use with ',r'.
    ,y - (doesn't work) copy selected text to system clipboard (register *)
    ,yy - copy current line to system clipboard (register *)
    ,z - start global substitute (%s), first field is filled with word under cusor

    F1 - toggle displaying of 'hidden' white characters
    F2 - toggle comment/uncomment
    F3 - comment (even if line is commented already)
    S-F3 - uncomment
    F4 - fuzzy finder (FufFile)
    F5 - show function list and allow to go to this function (search with FindSubs() function)
    D-F5 - show list of occurrences of current word
    F6 - reload ~/.vimrc
    D-F6 - load current file
    F7 - show list of TODOs and FIXMEs from current file
    F8 - toggle camel case -> underscores of word under cursor
    F9 - toggle graphical undo
    S-F9 - toggle indent guides
    D-F9 - toggle wrap lines
    F10 - toggle paste, no paste

    > - indent
    < - deindent

    C-e - uruchom skrypt
    C-l - dopelnienie abbreviations

    Space - tworzy z zaznaczenia, lub zwija/rozwija istniejacy fold
    C-Space - wygodniejsze omnicompletion
    Home - przenosi nie na sztywno do pierwszej kolumny, tylko do pierwszej kolumny nie bedacej bialym znakiem
    C-CR - wstawia <br />
    S-Tab - wstawia 'prawdziwy' tabulator
    S-Up - przesun kursor linia w gore, wraz z przeskrolowaniem o ta linie pliku
    S-Down - przesun kursor linia w dol, wraz z przeskrolowaniem o ta linie pliku
    C-S-Up - przesun zaznaczone lub biezaca linie w gore
    C-S-Down - przesun zaznaczone lub biezaca linie w dol

Commands:
    W zastepuje 'w' - zapis pliku
